spring:
  datasource:
    url: jdbc:h2:mem:test_db
    username: system
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect